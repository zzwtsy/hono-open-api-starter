# 应用运行环境配置
# 可选值: development, production, test
NODE_ENV=production

# 服务器监听端口
# 默认: 3001
PORT=3001

# 日志级别配置
# 可选值: trace, debug, info, warn, error, fatal, silent
# 生产环境建议使用: info 或 warn
LOG_LEVEL=info

# 数据库连接配置
# 数据库主机地址
# 本地开发通常使用: localhost 或 127.0.0.1
DB_HOST=

# 数据库端口
# PostgreSQL 默认端口: 5432
# MySQL 默认端口: 3306
DB_PORT=

# 数据库名称
# 请替换为你的实际数据库名
DB_NAME=

# 数据库用户名
# 建议使用具有适当权限的专用用户
DB_USER=

# 数据库密码
# 建议使用强密码，避免使用特殊字符如 @, #, $ 等可能引起连接问题的字符
DB_PASSWORD=

# CORS 允许的域名列表
# 多个域名用英文逗号分隔，例如: https://example.com,https://app.example.com
# 设置为 * 表示允许所有域名（不推荐用于生产环境）
CORS_ORIGINS=
